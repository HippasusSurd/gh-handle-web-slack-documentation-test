{
	"name": "github-document-release-slack",
	"version": "4.0.0",
	"author": "Liam Pillay",
	"main": "index.js",
	"description": "Sends a release message to slack and creates a changelog from a pull request.",
	"scripts": {
		"build": "tsc && ncc build src/index.ts"
	},
	"dependencies": {
		"@actions/core": "^1.10.1",
		"@actions/github": "^6.0.0",
		"@vercel/ncc": "^0.38.1"
	},
	"devDependencies": {
		"@types/node": "^20.11.5",
		"typescript": "^5.3.3"
	}
}
